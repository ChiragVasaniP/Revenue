<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="24dp"
    style="@style/ThemeOverlay.GoogleAds.GenericDialog"
    app:cardBackgroundColor="?attr/genericDialogSurfaceColor"
    app:cardCornerRadius="24dp"
    app:cardElevation="10dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_horizontal"
        android:orientation="vertical"
        android:padding="24dp">

        <!-- Warning Icon -->
        <FrameLayout
            android:layout_width="56dp"
            android:layout_height="56dp"
            android:layout_marginBottom="16dp"
            android:background="@drawable/bg_circle_warning">

            <ImageView
                android:id="@+id/ivIcon"
                android:layout_width="24dp"
                android:layout_height="24dp"
                android:layout_gravity="center"
                android:src="@android:drawable/ic_dialog_alert"
                app:tint="?attr/genericDialogIconTintColor" />
        </FrameLayout>

        <!-- Title -->
        <TextView
            android:id="@+id/tvTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginBottom="8dp"
            android:text="Delete this item?"
            android:textColor="?attr/genericDialogTitleTextColor"
            android:textSize="20sp"
            android:textStyle="bold" />

        <!-- Description -->
        <TextView
            android:id="@+id/tvDescription"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginBottom="24dp"
            android:gravity="center"
            android:text="This action is permanent and cannot be undone. All associated data will be removed from our servers immediately."
            android:textColor="?attr/genericDialogDescriptionTextColor"
            android:textSize="14sp" />

        <!-- Delete Button -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnPositive"
            android:layout_width="match_parent"
            android:layout_height="56dp"
            android:layout_marginBottom="12dp"
            android:text="Delete"
            android:textAllCaps="false"
            android:textColor="?attr/genericDialogPositiveButtonTextColor"
            app:backgroundTint="?attr/genericDialogPositiveButtonColor"
            app:cornerRadius="28dp" />

        <!-- Cancel Button -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnNegative"
            android:layout_width="match_parent"
            android:layout_height="56dp"
            android:text="Cancel"
            android:textAllCaps="false"
            android:textColor="?attr/genericDialogNegativeButtonTextColor"
            app:backgroundTint="?attr/genericDialogNegativeButtonColor"
            app:cornerRadius="28dp" />

    </LinearLayout>

</com.google.android.material.card.MaterialCardView>
